---
phase: 03-core-visualizations-and-launch
plan: 03
type: execute
wave: 2
depends_on: [03-01, 03-02]
files_modified:
  - site/src/App.tsx
  - site/src/styles/main.css
autonomous: false
requirements: [GRPH-04, DSGN-07]

must_haves:
  truths:
    - "The commit graph renders directly below the hero section using real R2 data"
    - "The project timeline renders below the commit graph with real R2 data"
    - "Hover states work on all interactive elements: graph cells, project cards, links, date spine months, navbar contact button, footer links"
    - "The full page layout flows: hero -> commit graph -> divider -> timeline -> footer"
    - "Loading and error states are handled gracefully for both visualizations"
  artifacts:
    - path: "site/src/App.tsx"
      provides: "Full page composition with CommitGraph and Timeline receiving R2 data"
  key_links:
    - from: "site/src/App.tsx"
      to: "site/src/components/CommitGraph/CommitGraph.tsx"
      via: "renders CommitGraph with r2State.data.graph"
      pattern: "CommitGraph.*data.*graph"
    - from: "site/src/App.tsx"
      to: "site/src/components/Timeline/Timeline.tsx"
      via: "renders Timeline with r2State.data.projects"
      pattern: "Timeline.*data.*projects"
---

<objective>
Integrate CommitGraph and Timeline into the main App layout, wire them to R2 data, apply global hover states to existing interactive elements (navbar, footer links), and verify the complete page visually.

Purpose: This plan connects all the pieces -- the visualizations from plans 01+02, the data from R2, and the existing page shell -- into the final product.
Output: Complete single-scroll page layout with live data flowing through all components.
</objective>

<execution_context>
@/Users/jacklabbe/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jacklabbe/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-core-visualizations-and-launch/03-CONTEXT.md
@.planning/phases/03-core-visualizations-and-launch/03-01-SUMMARY.md
@.planning/phases/03-core-visualizations-and-launch/03-02-SUMMARY.md
@site/src/App.tsx
@site/src/styles/main.css
@site/src/components/Footer/Footer.tsx
@site/src/components/Navbar/Navbar.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integrate visualizations into App.tsx and apply global hover states</name>
  <files>
    site/src/App.tsx
    site/src/styles/main.css
  </files>
  <action>
Update `App.tsx`:
1. Import CommitGraph and Timeline components
2. Remove the two SectionPlaceholder usages (the "// activity" and "// projects" placeholders)
3. Remove the SectionPlaceholder import if no longer used
4. Replace "// activity" placeholder with:
   - Conditionally render `<CommitGraph data={r2State.data.graph} />` when `r2State.status === 'loaded' && r2State.data.graph`
   - Wrap in the same stagger pattern: `<div className={stagger(isVisible(5))}>`
   - The CommitGraph sits directly below the hero (GRPH-04) -- no section heading per user decision
5. Keep the Divider between commit graph and timeline
6. Replace "// projects" placeholder with:
   - Conditionally render `<Timeline projects={r2State.data.projects} />` when `r2State.status === 'loaded' && r2State.data.projects`
   - Wrap in stagger: `<div className={stagger(isVisible(7))}>`
7. Handle error/loading states: if r2State.status is 'error' or data is null, render a minimal fallback (e.g., empty div or a subtle "Data unavailable" message in text-text-dim font-mono text-sm -- Claude's discretion on exact style)
8. Final layout order: SkipToContent -> Navbar -> main(Hero -> CommitGraph -> Divider -> Timeline) -> Footer

Apply global hover states to existing components (DSGN-07 completion):
- Read Footer.tsx and Navbar.tsx to identify existing link elements
- Add `link-hover` class to footer social links and any other anchor/button elements that should have the color-brighten + underline-slide-in effect
- For the Navbar contact button: if it already has hover styles via Tailwind, ensure they use 120ms timing. If not, add appropriate hover:text-accent transition.
- This ensures DSGN-07 covers ALL interactive elements across the site, not just the new components.

In `main.css`: if any additional global hover utility classes are needed for existing elements (e.g., footer icon links that may need a different hover treatment like opacity change rather than underline), add them. The link-hover class from plan 02 handles text links. Icon-only links in the footer may just need `hover:text-accent transition-colors duration-[120ms]` applied directly via Tailwind classes.
  </action>
  <verify>
Run `cd /Users/jacklabbe/Projects/website-v2 && npx tsc --noEmit -p site/tsconfig.json` -- no type errors. Run `cd /Users/jacklabbe/Projects/website-v2/site && npm run build` (or `pnpm build`) to confirm production build succeeds. Verify App.tsx no longer imports SectionPlaceholder (unless still used elsewhere). Verify CommitGraph and Timeline are conditionally rendered with R2 data.
  </verify>
  <done>
App.tsx composes the full page: hero -> commit graph (with R2 graph data) -> divider -> timeline (with R2 projects data) -> footer. SectionPlaceholders removed. Global hover states applied to all interactive elements. Production build succeeds.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of complete page</name>
  <files>site/src/App.tsx</files>
  <action>
Human visual verification of the complete portfolio page. Claude has already built and integrated all components. The user needs to verify the visual result in the browser.

What was built:
- Commit heatmap (blue color scale, tooltip, legend, wave animation)
- Project timeline (month-grouped cards, date spine, scroll sync)
- Hover states on all interactive elements
- Full single-scroll layout with real R2 data

How to verify:
1. Run `cd /Users/jacklabbe/Projects/website-v2/site && pnpm dev` and open the local URL
2. Commit Graph: Verify blue heatmap renders below hero with no section heading. Hover over cells -- tooltip shows "N commits on Date", cell scales up. Check Less-More legend visible. Check month labels on top, day labels on left. Resize to mobile width -- heatmap scrolls horizontally. Reload page -- watch for left-to-right wave animation.
3. Timeline: Scroll down past the divider -- projects appear grouped by month. Verify public repos have clickable name links (open in new tab). Verify private repos show "Private Repo" with accent border, no link. Check language badges have correct GitHub colors. Check commit count and date range display.
4. Date Spine: Verify sticky sidebar on right with month labels. Scroll through content -- active month highlights in blue/bold. Click a month -- smooth scroll to that section. Resize to mobile -- months show abbreviated format.
5. Hover States: Hover project cards -- lift + background shift. Hover repo name links -- color brightens + underline slides in. Hover date spine months -- text brightens. Hover footer links/icons -- color change. Hover navbar contact button -- appropriate feedback.
6. General: Full layout flows hero -> graph -> divider -> timeline -> footer. No layout overflow issues, no broken sticky positioning.
  </action>
  <verify>User visually confirms all elements render correctly in the browser at both desktop and mobile widths.</verify>
  <done>User types "approved" confirming the complete portfolio page looks and functions as specified.</done>
</task>

</tasks>

<verification>
- Production build succeeds (`pnpm build` in site/)
- App.tsx imports and renders CommitGraph and Timeline with R2 data
- SectionPlaceholder removed from layout
- All interactive elements have hover states
- Page layout: hero -> commit graph -> divider -> timeline -> footer
</verification>

<success_criteria>
- Complete portfolio page renders with live R2 data
- Commit graph and timeline are visually polished and interactive
- All hover states work across the entire site (graph, cards, links, spine, nav, footer)
- User approves the visual result
</success_criteria>

<output>
After completion, create `.planning/phases/03-core-visualizations-and-launch/03-03-SUMMARY.md`
</output>
