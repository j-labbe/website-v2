---
phase: 02-site-shell-design-system-and-hero
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - site/src/components/Hero/Hero.tsx
  - site/src/components/Hero/Hero.module.css
  - site/src/components/Hero/HeroSkeleton.tsx
  - site/src/components/Hero/HeroSkeleton.module.css
  - site/src/components/Navbar/Navbar.tsx
  - site/src/components/Navbar/Navbar.module.css
  - site/src/components/SkipToContent/SkipToContent.tsx
  - site/src/components/SkipToContent/SkipToContent.module.css
  - site/src/components/icons/SocialIcons.tsx
autonomous: true
requirements:
  - HERO-01
  - HERO-02
  - HERO-03
  - HERO-04
  - DSGN-04
  - DSGN-05
  - META-02
  - META-03

must_haves:
  truths:
    - "Hero section displays photo on left, name 'Jack Labbe' (large bold Inter), tagline 'Software / AI Engineer' (lighter Inter), and contact button (filled accent pill, mailto) on right"
    - "Hero has a subtle radial gradient background with accent glow, generous whitespace, and content vertically centered"
    - "On mobile (<=768px), hero photo restacks on top with text below"
    - "Navbar is sticky with glass/backdrop-blur effect, contains only a contact button (filled accent pill, top-right)"
    - "Skip-to-content link is visually hidden until focused, targets #main-content"
    - "HeroSkeleton renders content-shaped shimmer placeholders matching the real hero layout (circle for photo, text bars, button shape)"
    - "Staggered entrance animation (photo -> name -> tagline -> button) with ~100ms gaps, respects prefers-reduced-motion"
  artifacts:
    - path: "site/src/components/Hero/Hero.tsx"
      provides: "Hero section with split layout, name, tagline, contact button"
      min_lines: 30
    - path: "site/src/components/Hero/Hero.module.css"
      provides: "Hero styles with radial gradient, responsive layout, stagger animation"
      contains: "radial-gradient"
    - path: "site/src/components/Hero/HeroSkeleton.tsx"
      provides: "Skeleton loading state matching hero layout"
      min_lines: 15
    - path: "site/src/components/Navbar/Navbar.tsx"
      provides: "Sticky navbar with contact button"
      min_lines: 15
    - path: "site/src/components/Navbar/Navbar.module.css"
      provides: "Navbar styles with backdrop-filter blur"
      contains: "backdrop-filter"
    - path: "site/src/components/SkipToContent/SkipToContent.tsx"
      provides: "Accessible skip-to-content link"
      min_lines: 5
    - path: "site/src/components/icons/SocialIcons.tsx"
      provides: "Inline SVG icon components for GitHub, LinkedIn, Email, X"
      exports: ["GitHubIcon", "LinkedInIcon", "MailIcon", "XIcon"]
  key_links:
    - from: "site/src/components/Hero/Hero.module.css"
      to: "site/src/styles/tokens.css"
      via: "CSS custom property references"
      pattern: "var\\(--"
    - from: "site/src/components/Navbar/Navbar.module.css"
      to: "site/src/styles/tokens.css"
      via: "CSS custom property references"
      pattern: "var\\(--"
    - from: "site/src/components/Hero/HeroSkeleton.tsx"
      to: "site/src/styles/animations.css"
      via: "skeleton shimmer class"
      pattern: "skeleton"
---

<objective>
Build all core UI components: Hero section (with skeleton), sticky Navbar, SkipToContent accessibility link, and social media icon components.

Purpose: These are the visual building blocks that App.tsx will compose in Plan 03. The hero section is the primary user-facing feature of this phase -- it displays the photo, name, tagline, and contact button with polished animations and responsive layout.
Output: Hero, HeroSkeleton, Navbar, SkipToContent, and SocialIcons components with their CSS Modules.
</objective>

<execution_context>
@/home/jlabbe/.claude/get-shit-done/workflows/execute-plan.md
@/home/jlabbe/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-site-shell-design-system-and-hero/02-CONTEXT.md
@.planning/phases/02-site-shell-design-system-and-hero/02-RESEARCH.md
@.planning/phases/02-site-shell-design-system-and-hero/02-01-SUMMARY.md
@site/src/styles/tokens.css
@site/src/styles/animations.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Hero section component with skeleton and staggered animation</name>
  <files>
    site/src/components/Hero/Hero.tsx
    site/src/components/Hero/Hero.module.css
    site/src/components/Hero/HeroSkeleton.tsx
    site/src/components/Hero/HeroSkeleton.module.css
  </files>
  <action>
    Create the Hero component implementing ALL locked decisions from CONTEXT.md:

    **Hero.tsx:**
    - Props: `{ isLoading: boolean }` to toggle between skeleton and real content
    - Semantic HTML: `<section>` with class for the hero, `<div>` container with max-width
    - Layout: Flexbox row with ~50/50 split. Photo on LEFT, text content on RIGHT.
    - Photo: `<img src="/headshot.webp" alt="Jack Labbe" />` wrapped in a styled div. Use rounded rectangle with border-radius: var(--radius) (8px), object-fit: cover, ~280-320px wide with aspect-ratio approximately 4:5 portrait.
    - Name: `<h1>` with "Jack Labbe" -- Inter bold, font-size var(--text-5xl) desktop / var(--text-4xl) mobile, color var(--text-bright)
    - Tagline: `<p>` with "Software / AI Engineer" -- Inter lighter weight (300-400), NOT monospace per locked decision, font-size var(--text-xl), color var(--text)
    - Contact button: `<a href="mailto:contact@jacklabbe.com">` with text "Contact" -- filled accent (#4F7DF5), pill shape (border-radius: var(--radius-pill)), text color var(--bg) (DARK text, not white -- per WCAG audit: white on accent is only 3.76:1 FAIL, dark is 5.15:1 PASS), font-weight 600
    - Staggered entrance animation: When `isLoading` transitions from true to false, apply the staggerItem class from animations.css with --stagger-index CSS variable: photo=0, name=1, tagline=2, button=3. Use React.CSSProperties cast for the CSS variable: `style={{ '--stagger-index': N } as React.CSSProperties}`. Elements should start with opacity:0 and only animate when NOT loading.
    - Content vertically centered (align-items: center on the flex container)

    **Hero.module.css:**
    - .hero: background with radial-gradient(ellipse at 70% 50%, var(--accent-glow), var(--bg) 70%). Padding: var(--space-12) 0 (generous whitespace per HERO-04). NOT full viewport height.
    - .container: max-width var(--max-width), margin 0 auto, padding 0 var(--space-6), display flex, align-items center, gap var(--space-10)
    - .photo: flex basis ~50%, max-width 320px. img inside: width 100%, border-radius var(--radius), object-fit cover, aspect-ratio 4/5
    - .content: flex 1, display flex, flex-direction column
    - .name: font-family var(--font-sans), font-weight 700, font-size var(--text-5xl), color var(--text-bright), line-height 1.1
    - .tagline: font-family var(--font-sans) (NOT monospace), font-weight 300, font-size var(--text-xl), color var(--text), margin-top var(--space-3)
    - .contactButton: inline-flex, padding var(--space-3) var(--space-6), background var(--accent), color var(--bg), border-radius var(--radius-pill), font-family var(--font-sans), font-weight 600, font-size var(--text-base), text-decoration none, margin-top var(--space-5), transition opacity var(--transition-fast)
    - .contactButton:hover: opacity 0.85
    - .contactButton:focus-visible: outline 2px solid var(--accent-secondary), outline-offset 2px

    **Mobile responsive (max-width: 768px):**
    - .container: flex-direction column, text-align center
    - .photo: max-width 200px, margin 0 auto
    - .name: font-size var(--text-4xl)

    **HeroSkeleton.tsx:**
    - Renders the same layout structure as Hero but with skeleton placeholders
    - Photo placeholder: rounded div with skeleton shimmer class, same dimensions as real photo
    - Name placeholder: rectangle bar (~60% width, height matching text-5xl) with skeleton class
    - Tagline placeholder: rectangle bar (~40% width, height matching text-xl) with skeleton class
    - Button placeholder: pill-shaped div (~120px wide) with skeleton class
    - Import skeleton class from animations.css or define locally -- the shimmer animation is in animations.css as a global class

    **HeroSkeleton.module.css:**
    - Same layout as Hero.module.css (reuse via shared container classes or duplicate the structural rules)
    - Skeleton elements: specific dimensions matching real content placeholders
  </action>
  <verify>
    Run `cd /home/jlabbe/personal/website && pnpm build` -- zero errors. Verify Hero.tsx exports a component. Verify HeroSkeleton.tsx exports a component. Verify Hero.module.css contains radial-gradient and responsive breakpoint. Verify no white text on accent buttons (color should be var(--bg)).
  </verify>
  <done>
    Hero component renders a right-aligned split layout with photo (left), text content (right), radial gradient background, staggered entrance animation, and responsive mobile restack. HeroSkeleton renders content-shaped shimmer placeholders matching the hero layout. Contact button uses dark text on accent background (WCAG AA compliant). Both components compile without errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Navbar, SkipToContent, and social icon components</name>
  <files>
    site/src/components/Navbar/Navbar.tsx
    site/src/components/Navbar/Navbar.module.css
    site/src/components/SkipToContent/SkipToContent.tsx
    site/src/components/SkipToContent/SkipToContent.module.css
    site/src/components/icons/SocialIcons.tsx
  </files>
  <action>
    **Navbar.tsx:**
    - Sticky navbar with ONLY a contact button (no name, no branding, no logo -- locked decision)
    - Semantic HTML: `<nav>` element with aria-label="Main navigation"
    - Inner container: max-width var(--max-width), centered, flex with justify-content: flex-end
    - Contact button: same style as hero -- filled accent (#4F7DF5), pill shape, dark text (var(--bg)), mailto link. Reuse the same styling approach as Hero's contact button.
    - Glass / backdrop blur effect: background rgba(4, 13, 33, 0.8), backdrop-filter: blur(12px), -webkit-backdrop-filter: blur(12px) for Safari
    - Scroll detection: use a small useEffect with IntersectionObserver or scroll event to detect when page has scrolled past top. Set `data-scrolled="true"` attribute on the nav element when scrolled. This controls the bottom border visibility (transparent when at top, var(--border) when scrolled).
    - z-index: 100 to layer above content

    **Navbar.module.css:**
    - .navbar: position sticky, top 0, z-index 100, background rgba(4, 13, 33, 0.8), backdrop-filter blur(12px), -webkit-backdrop-filter blur(12px), border-bottom 1px solid transparent, transition border-color var(--transition-base), padding var(--space-4) 0
    - .navbar[data-scrolled="true"]: border-bottom-color var(--border)
    - .inner: max-width var(--max-width), margin 0 auto, padding 0 var(--space-6), display flex, justify-content flex-end, align-items center
    - .contactButton: same styling as hero contact button (inline-flex, accent bg, dark text, pill radius, padding, font-weight 600, font-size var(--text-sm) -- slightly smaller than hero button)
    - .contactButton:hover: opacity 0.85
    - .contactButton:focus-visible: outline 2px solid var(--accent-secondary), outline-offset 2px

    **SkipToContent.tsx:**
    - Renders `<a href="#main-content" className={styles.skipLink}>Skip to content</a>`
    - Visually hidden by default (position absolute, top -100%)
    - Becomes visible on :focus (top var(--space-4))
    - z-index: 999 to appear above everything including navbar

    **SkipToContent.module.css:**
    - .skipLink: position absolute, top -100%, left var(--space-4), z-index 999, padding var(--space-3) var(--space-5), background var(--accent), color var(--bg), border-radius var(--radius), font-weight 600, transition top var(--transition-fast)
    - .skipLink:focus: top var(--space-4)

    **SocialIcons.tsx:**
    - Four inline SVG icon components: GitHubIcon, LinkedInIcon, MailIcon, XIcon
    - Props interface: `{ size?: number; className?: string }`
    - Default size: 20
    - Style: outline/stroke (not filled) -- `fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round"`
    - All icons include `aria-hidden="true"` since they will be wrapped in links with accessible labels
    - Use the SVG paths from the research document (02-RESEARCH.md Code Examples section)
    - Export all four as named exports
  </action>
  <verify>
    Run `cd /home/jlabbe/personal/website && pnpm build` -- zero errors. Verify all component files exist. Verify Navbar.module.css contains backdrop-filter. Verify SocialIcons.tsx exports all four icon components. Verify SkipToContent targets #main-content.
  </verify>
  <done>
    Navbar renders as a sticky glass-effect bar with only a contact button (accent pill, dark text). Bottom border appears on scroll. SkipToContent link is hidden until focused, targets #main-content. All four social icon SVG components exported. All components use CSS Modules with design token references. `pnpm build` passes.
  </done>
</task>

</tasks>

<verification>
1. `pnpm build` passes from repo root with zero errors
2. Hero displays split layout: photo left, text right
3. Hero contact button has dark text (var(--bg)) on accent background, NOT white text
4. Hero tagline uses Inter (var(--font-sans)), NOT monospace
5. Navbar has backdrop-filter AND -webkit-backdrop-filter
6. Navbar has only a contact button, no branding
7. SkipToContent href targets #main-content
8. All CSS Modules reference tokens via var(--token), no hard-coded color values
9. prefers-reduced-motion respected in animations
10. Responsive breakpoint at 768px for mobile restack
</verification>

<success_criteria>
- All 5 component directories created with .tsx + .module.css pairs
- Hero renders the locked split layout with correct typography, colors, and responsive behavior
- HeroSkeleton mirrors the hero layout with shimmer placeholders
- Navbar is sticky with glass blur and scroll-aware border
- SkipToContent is accessible (hidden until focus)
- Social icons are inline SVGs with no dependencies
- Build passes with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-site-shell-design-system-and-hero/02-02-SUMMARY.md`
</output>
