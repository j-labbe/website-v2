---
phase: 02-site-shell-design-system-and-hero
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - site/package.json
  - site/vite.config.ts
  - site/src/vite-env.d.ts
  - site/src/styles/tokens.css
  - site/src/styles/reset.css
  - site/src/styles/fonts.css
  - site/src/styles/global.css
  - site/src/styles/animations.css
  - site/src/main.tsx
  - site/src/hooks/useR2Data.ts
  - site/src/hooks/useFontsReady.ts
  - site/.env
  - site/.env.production
autonomous: true
requirements:
  - DSGN-01
  - DSGN-02
  - DSGN-03
  - DSGN-06
  - DSGN-09
  - DSGN-10
  - DSGN-11
  - INFR-05

must_haves:
  truths:
    - "All design tokens (colors, spacing, typography, layout) are defined as CSS custom properties in a single tokens.css file"
    - "Inter and JetBrains Mono variable fonts are self-hosted via Fontsource and available throughout the site"
    - "The R2 data fetching hook loads graph.json, projects.json, and meta.json in parallel with 5s timeout"
    - "The crosshatch grid pattern is rendered at low opacity and toggleable via --grid-visible CSS variable"
    - "CSS Modules work with camelCase imports (Vite localsConvention configured)"
  artifacts:
    - path: "site/src/styles/tokens.css"
      provides: "All design tokens as CSS custom properties"
      contains: "--bg: #040d21"
    - path: "site/src/styles/fonts.css"
      provides: "Font imports from Fontsource"
      contains: "@fontsource-variable/inter"
    - path: "site/src/styles/reset.css"
      provides: "Minimal CSS reset"
      contains: "box-sizing: border-box"
    - path: "site/src/styles/global.css"
      provides: "Global body styles and crosshatch grid overlay"
      contains: "--grid-visible"
    - path: "site/src/styles/animations.css"
      provides: "Shared keyframe animations (shimmer, fadeInUp)"
      contains: "@keyframes shimmer"
    - path: "site/src/hooks/useR2Data.ts"
      provides: "R2 data fetching with parallel fetch, timeout, error handling"
      exports: ["useR2Data"]
    - path: "site/src/hooks/useFontsReady.ts"
      provides: "Font loading detection"
      exports: ["useFontsReady"]
  key_links:
    - from: "site/src/hooks/useR2Data.ts"
      to: "@jacklabbe/shared"
      via: "type imports for GraphData, ProjectsFile, PipelineMeta"
      pattern: "import type.*from '@jacklabbe/shared'"
    - from: "site/src/hooks/useR2Data.ts"
      to: "VITE_R2_BASE_URL"
      via: "import.meta.env.VITE_R2_BASE_URL"
      pattern: "import\\.meta\\.env\\.VITE_R2_BASE_URL"
    - from: "site/src/main.tsx"
      to: "site/src/styles/*.css"
      via: "CSS import chain"
      pattern: "import.*styles/"
---

<objective>
Establish the complete design system foundation (CSS tokens, typography, animations, global styles) and data fetching infrastructure for the site SPA.

Purpose: Every component in Plans 02 and 03 depends on design tokens, font availability, and the data hook. This plan creates the shared foundation that all subsequent UI work builds upon.
Output: CSS design token system, font loading, animations, R2 data hook, font-ready hook, Vite config, environment variables.
</objective>

<execution_context>
@/home/jlabbe/.claude/get-shit-done/workflows/execute-plan.md
@/home/jlabbe/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-site-shell-design-system-and-hero/02-CONTEXT.md
@.planning/phases/02-site-shell-design-system-and-hero/02-RESEARCH.md
@shared/src/index.ts
@shared/src/graph.ts
@shared/src/projects.ts
@shared/src/meta.ts
@site/vite.config.ts
@site/package.json
@site/src/main.tsx
@site/src/vite-env.d.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install fonts, configure Vite, create design system CSS files</name>
  <files>
    site/package.json
    site/vite.config.ts
    site/src/vite-env.d.ts
    site/src/styles/tokens.css
    site/src/styles/reset.css
    site/src/styles/fonts.css
    site/src/styles/global.css
    site/src/styles/animations.css
    site/src/main.tsx
    site/.env
    site/.env.production
  </files>
  <action>
    1. Install font dependencies: `cd site && pnpm add @fontsource-variable/inter @fontsource-variable/jetbrains-mono`

    2. Update `site/vite.config.ts` to add CSS Modules configuration:
       - Add `css.modules.localsConvention: 'camelCaseOnly'` per research recommendation

    3. Update `site/src/vite-env.d.ts` to add `VITE_R2_BASE_URL` type declaration:
       - Add `ImportMetaEnv` interface with `readonly VITE_R2_BASE_URL: string`
       - Add `ImportMeta` interface override

    4. Create `site/.env` with `VITE_R2_BASE_URL` pointing to the R2 public dev URL (use placeholder `https://pub-XXXXXXXX.r2.dev` -- user will fill in real value)
    5. Create `site/.env.production` with `VITE_R2_BASE_URL=https://data.jacklabbe.com`
    6. Add `.env` and `.env.production` to the site .gitignore (env files should not be committed -- they contain environment-specific URLs). Also add a `.env.example` with the variable name for documentation.

    7. Create `site/src/styles/tokens.css` with ALL design tokens as CSS custom properties on `:root`:
       - Colors (LOCKED): --bg: #040d21, --surface: #0a1628, --surface-2: #111d33, --border: #1a2844, --text: #c9d1d9, --text-dim: #6e7a8a, --text-bright: #f0f6fc, --accent: #4F7DF5, --accent-secondary: #6B9CFF, --accent-glow: rgba(79, 125, 245, 0.15)
       - Semantic colors: --success: #3fb950, --warning: #d29922, --error: #f85149
       - Typography: --font-sans (Inter Variable fallback chain), --font-mono (JetBrains Mono Variable fallback chain)
       - Type scale: --text-xs (0.75rem) through --text-5xl (4rem) as defined in research
       - Spacing scale: --space-1 (0.25rem) through --space-16 (8rem) on 4px grid
       - Layout: --max-width: 1200px, --radius: 8px, --radius-pill: 9999px
       - Decorative: --grid-visible: 0 (default off)
       - Transitions: --transition-fast: 150ms ease, --transition-base: 250ms ease

    8. Create `site/src/styles/reset.css` -- minimal ~20-line reset:
       - box-sizing: border-box on *, *::before, *::after
       - margin/padding reset on *
       - html: antialiased font rendering
       - body: font-family var(--font-sans), color var(--text), background var(--bg), line-height 1.6, min-height 100vh
       - img, svg: display block, max-width 100%
       - a: color inherit, no decoration
       - button: font inherit, color inherit, no background/border, cursor pointer

    9. Create `site/src/styles/fonts.css`:
       - `@import '@fontsource-variable/inter';`
       - `@import '@fontsource-variable/jetbrains-mono';`

    10. Create `site/src/styles/global.css`:
        - body::before pseudo-element for crosshatch grid overlay:
          - position: fixed, inset: 0, pointer-events: none, z-index: 0
          - opacity: calc(var(--grid-visible) * 0.04)
          - SVG crosshatch background-image (data URI) at 40px grid size
          - transition: opacity var(--transition-base)
        - Ensure body position: relative so pseudo-element layers correctly

    11. Create `site/src/styles/animations.css`:
        - @keyframes shimmer: background-position sweep from 200% to -200%
        - .skeleton class: linear-gradient shimmer on surface colors, 1.5s infinite
        - @keyframes fadeInUp: from opacity 0 + translateY(12px), to opacity 1 + translateY(0)
        - .staggerItem class: opacity 0, fadeInUp 400ms ease-out forwards, delay via calc(var(--stagger-index, 0) * 100ms)
        - @media (prefers-reduced-motion: reduce) blocks for BOTH animations: disable animation, set opacity: 1, transform: none

    12. Update `site/src/main.tsx` to import all CSS files in correct order:
        - import './styles/reset.css'
        - import './styles/tokens.css'
        - import './styles/fonts.css'
        - import './styles/global.css'
        - import './styles/animations.css'
        - Keep existing React render code

    CRITICAL CONTRAST NOTES (from WCAG audit in research):
    - --text-dim (#6e7a8a) FAILS AA for normal text on all backgrounds. Add a CSS comment in tokens.css: "/* WARNING: --text-dim fails AA for normal text. Use only for large text (>=18.66px bold, >=24px regular) or decorative text */"
    - Button text on --accent: use --bg (dark), NOT white. White on #4F7DF5 is only 3.76:1 (FAIL). Dark on #4F7DF5 is 5.15:1 (PASS).
    - Use --accent-secondary (#6B9CFF) for links/interactive text (7.21:1 on --bg = PASS).
  </action>
  <verify>
    Run `cd /home/jlabbe/personal/website && pnpm build` -- should complete with zero errors. Verify all CSS files exist under site/src/styles/. Run `cd /home/jlabbe/personal/website/site && pnpm dev` briefly and confirm the dev server starts (Ctrl+C after confirming).
  </verify>
  <done>
    All 5 CSS files exist with design tokens. Vite CSS Modules configured with camelCaseOnly. Fonts installed. Environment variables typed. `pnpm build` succeeds. The full design token system is ready for components to consume via var(--token) references.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create R2 data fetching hook and font loading hook</name>
  <files>
    site/src/hooks/useR2Data.ts
    site/src/hooks/useFontsReady.ts
  </files>
  <action>
    1. Create `site/src/hooks/useR2Data.ts`:
       - Import shared types: `import type { GraphData, ProjectsFile, PipelineMeta } from '@jacklabbe/shared'`
       - Define `R2Data` interface: `{ graph: GraphData | null; projects: ProjectsFile | null; meta: PipelineMeta | null }`
       - Define `R2State` discriminated union: `{ status: 'loading' } | { status: 'loaded'; data: R2Data } | { status: 'error'; error: string }`
       - Read base URL from `import.meta.env.VITE_R2_BASE_URL`
       - Implement `fetchWithTimeout(url: string, timeoutMs: number)`: uses AbortController with 5000ms timeout, throws on non-ok response
       - Implement `useR2Data()` hook:
         - useState with initial `{ status: 'loading' }`
         - useEffect that fetches all three JSON files in parallel via Promise.all: `${R2_BASE}/graph.json`, `${R2_BASE}/projects.json`, `${R2_BASE}/meta.json`
         - Parse all three responses as JSON with proper type assertions
         - On success: set `{ status: 'loaded', data: { graph, projects, meta } }`
         - On error: set `{ status: 'error', error: message }` -- handle AbortError as "Request timed out"
         - Cleanup: set cancelled flag, abort controller on unmount
         - Optional sessionStorage caching (discretion): check sessionStorage for cached data with timestamp. If fresh (<1 hour), return immediately. On successful fetch, cache to sessionStorage. This improves repeat visits.
       - Export the hook and the R2State type

    2. Create `site/src/hooks/useFontsReady.ts`:
       - Implement `useFontsReady()` hook:
         - useState(false) for ready state
         - useEffect that calls `document.fonts.ready.then(() => setReady(true))`
         - Return boolean
       - Export the hook

    Both hooks must import useState and useEffect from 'react'.
  </action>
  <verify>
    Run `cd /home/jlabbe/personal/website && pnpm build` -- should complete with zero errors including type checking of the hooks against shared types. Verify the hook files exist and export the expected names.
  </verify>
  <done>
    useR2Data hook fetches all 3 R2 JSON files in parallel with 5s timeout, returns loading/loaded/error states typed against shared types. useFontsReady hook detects font loading completion via document.fonts.ready. Both hooks type-check against the shared package types. `pnpm build` passes.
  </done>
</task>

</tasks>

<verification>
1. `pnpm build` passes from repo root with zero errors
2. All 5 CSS files exist under site/src/styles/ with correct token values
3. Fonts installed in site/node_modules/@fontsource-variable/
4. vite.config.ts has CSS Modules camelCaseOnly config
5. Both hooks exist and export correctly typed functions
6. Environment variable typed in vite-env.d.ts
7. main.tsx imports all CSS files in correct order
</verification>

<success_criteria>
- The design system foundation is complete: all tokens, fonts, reset, global styles, and animations defined as CSS custom properties
- Data hooks are ready to consume: useR2Data returns typed R2 state, useFontsReady returns boolean
- The build passes with zero errors, proving all types and imports resolve correctly
- WCAG contrast constraints are documented in token comments
</success_criteria>

<output>
After completion, create `.planning/phases/02-site-shell-design-system-and-hero/02-01-SUMMARY.md`
</output>
